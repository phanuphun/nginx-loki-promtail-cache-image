server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://10.0.1.220:20216/loki/api/v1/push

scrape_configs:
  - job_name: nginx-images
    static_configs:
      - targets: [localhost]
        labels:
          job: nginx-images
          vending: "VEDING20211564"
          __path__: /var/log/nginx/images_access.log